# =====================================================================
# zoos-upscale / .gitignore
# =====================================================================

# ---------------------------------------------------------------------
# 설계·계획 문서 — 폴더 단위로 통째 제외 (추적 금지)
#   이 저장소는 PUBLIC 이다. 설계 단계 산출물은 로컬에만 보관한다.
#   개별 파일 화이트리스트를 두지 않는다 (한 개만 새어 나가는 사고 방지).
#   공개 가능한 문서를 추적하려면 아래 /docs/ 항목을 명시적으로 조정할 것.
# ---------------------------------------------------------------------
/docs/
/planning/

# 프로젝트 지침 — 설계 요약을 포함하므로 계획 확정 시점까지 로컬 전용.
# 공개 결정이 나면 이 줄을 지우고 커밋한다.
/CLAUDE.md

# ---------------------------------------------------------------------
# Tauri · Rust · Svelte 빌드 산출물
# ---------------------------------------------------------------------
/target/
/node_modules/
/.pnpm-store/
/src-tauri/gen/
/src-tauri/binaries/*
!/src-tauri/binaries/.gitkeep
*.tsbuildinfo
pnpm-debug.log*

# ---------------------------------------------------------------------
# 모델 가중치·컴파일 산출물 (대용량 — Git LFS 또는 외부 배포)
#   models/catalog 의 manifest·라이선스 메타데이터는 추적한다.
# ---------------------------------------------------------------------
/models/**/*.onnx
/models/**/*.ort
/models/**/*.mlmodel
/models/**/*.mlmodelc/
/models/**/*.mlpackage/
/models/**/*.bin
/models/**/*.param
/models/**/*.engine
/models/**/*.plan
/models/**/*.trt
/models/**/*.pth
/models/**/*.pt
/models/**/*.safetensors

# ---------------------------------------------------------------------
# 런타임 작업 공간·상태 저장소
# ---------------------------------------------------------------------
/workspace/
/.cache/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
*.db-journal

# ---------------------------------------------------------------------
# 테스트 미디어·골든 산출물 (대용량)
# ---------------------------------------------------------------------
/tests/fixtures/media/
/tests/golden/out/

# ---------------------------------------------------------------------
# 패키징 산출물
# ---------------------------------------------------------------------
/packaging/**/dist/
/dist/
*.dmg
*.pkg
*.msi
*.appimage
*.AppImage
*.deb
*.rpm

# ---------------------------------------------------------------------
# 로그·진단
# ---------------------------------------------------------------------
*.log
/logs/
/crashdumps/

# ---------------------------------------------------------------------
# Python (모델 export/verify 도구 체인)
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------
# 에디터·도구·OS
# ---------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
.claude/settings.local.json

# ---------------------------------------------------------------------
# 시크릿·서명 자재
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
*.p12
*.pfx
*.mobileprovision
*.keystore
