# =====================================================================
# zoos-upscale / .gitignore
# =====================================================================

# ---------------------------------------------------------------------
# 로컬 전용 계획·설계 문서 (추적하지 않음)
#   공개 저장소이므로 계획 단계 산출물은 로컬에만 보관한다.
#   확정된 아키텍처 문서는 이후 docs/ 로 옮겨 추적한다.
# ---------------------------------------------------------------------
/planning/

# ---------------------------------------------------------------------
# 빌드 산출물 (CMake)
# ---------------------------------------------------------------------
/build/
/build-*/
/out/
/cmake-build-*/
CMakeUserPresets.json
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
install_manifest.txt
compile_commands.json
Testing/
*.o
*.obj
*.a
*.lib
*.so
*.so.*
*.dylib
*.dll
*.exe
*.pdb
*.ilk
*.exp

# ---------------------------------------------------------------------
# vcpkg
# ---------------------------------------------------------------------
/vcpkg/
/vcpkg_installed/
.vcpkg-root

# ---------------------------------------------------------------------
# Qt 6 생성물
# ---------------------------------------------------------------------
*.moc
moc_*.cpp
qrc_*.cpp
ui_*.h
*.qmlc
*.jsc
.qmake.stash
*.pro.user
*.pro.user.*

# ---------------------------------------------------------------------
# 모델 가중치·컴파일 산출물 (대용량 — Git LFS 또는 외부 배포)
#   models/catalog 의 manifest·라이선스 메타데이터는 추적한다.
# ---------------------------------------------------------------------
/models/**/*.onnx
/models/**/*.ort
/models/**/*.mlmodel
/models/**/*.mlmodelc/
/models/**/*.mlpackage/
/models/**/*.bin
/models/**/*.param
/models/**/*.engine
/models/**/*.plan
/models/**/*.trt
/models/**/*.pth
/models/**/*.pt
/models/**/*.safetensors

# ---------------------------------------------------------------------
# 런타임 작업 공간·상태 저장소
# ---------------------------------------------------------------------
/workspace/
/.cache/
*.sqlite
*.sqlite-wal
*.sqlite-shm
*.db
*.db-journal

# ---------------------------------------------------------------------
# 테스트 미디어·골든 산출물 (대용량)
# ---------------------------------------------------------------------
/tests/fixtures/media/
/tests/golden/out/

# ---------------------------------------------------------------------
# 패키징 산출물
# ---------------------------------------------------------------------
/packaging/**/dist/
/dist/
*.dmg
*.pkg
*.msi
*.appimage
*.AppImage
*.deb
*.rpm

# ---------------------------------------------------------------------
# 로그·진단
# ---------------------------------------------------------------------
*.log
/logs/
/crashdumps/

# ---------------------------------------------------------------------
# Python (모델 export/verify 도구 체인)
# ---------------------------------------------------------------------
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ---------------------------------------------------------------------
# 에디터·도구·OS
# ---------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db
.claude/settings.local.json

# ---------------------------------------------------------------------
# 시크릿·서명 자재
# ---------------------------------------------------------------------
.env
.env.*
!.env.example
*.p12
*.pfx
*.mobileprovision
*.keystore
