[workspace]
members = ["src-tauri"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2024"
rust-version = "1.96"
license = "Apache-2.0"
repository = "https://github.com/zooslab/zoos-upscale"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = true
